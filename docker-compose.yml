version: '3'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - 80:80
  frontend:
    build:
      dockerfile: Dockerfile.frontend
    ports:
      - 4200:4200
